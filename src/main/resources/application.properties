#spring.application.name=taskmaster

## 1. Force a specific database name (No more random UUIDs!)
#spring.datasource.url=jdbc:h2:mem:taskdb
#
## 2. Default username (Spring does this anyway, but good to be explicit)
#spring.datasource.username=sa
#spring.datasource.password=
#
## 3. Enable the H2 Console so you can see it in the browser
#spring.h2.console.enabled=true

#spring.datasource.url=jdbc:postgresql://localhost:5432/TaskMaster
#spring.datasource.username=postgres
#spring.datasource.password=${DB_PASSWORD}
#
#//hibernate
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.show-sql=true

# ========================
# PRODUCTION CONFIGURATION
# ========================

# PostgreSQL Database (values will come from Render/Neon environment variables)
spring.application.name=taskmaster

# Database - reads from environment variables
# Locally: set env vars in IntelliJ Run Config
# On Render: set env vars in Render Dashboard
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/TaskMaster}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=${PORT:8080}

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}

# JWT Configuration
jwt.secret=${JWT_SECRET:X9f2kLm4nP7qRs1tUv3wYz5aB8cD0eF6gH2iJ4kL7mN9oP1qR3sT5uV7wX9yZ1}
jwt.expiration=${JWT_EXPIRATION:86400000}